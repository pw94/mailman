<%= simple_form_for @message do |f| %>
  <%= f.input :subject %>
  <%= f.input :body, as: :text %>
  <%= f.association :records, collection: current_user.owned_records.union(current_user.accessible_records), as: :check_boxes %>
  <%= f.button :submit %>
<% end %>